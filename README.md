# autonomy-demo-scratch

Throwaway scratch consumer repo for autonomy-plugin conforming-path demos
(WP3 return-accounting; WP4 trigger-dispatch). Installs the work-items
capability's tracker seam (`tools/work-item-tracker/`, copied from a carrier
repo per its own install path) and a repo-local autonomy binding with a
`triggers` section. Not production anything.
