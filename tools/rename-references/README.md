# rename-references

Tier-0 rename detection for `/rename-references`: `detect-pair.sh` (git R-lines), `detect-sweep.sh` (pattern registry sweep). Pattern SSOT: `patterns.registry.tsv`; prose: the docs-hygiene plugin's rename-references patterns reference (`/docs-hygiene:rename-references`).
