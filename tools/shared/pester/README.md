# pester

Pester-run GitHub workflow annotation helper (`PesterWorkflowAnnotation.ps1`, dot-sourced into Pester invokers to emit `::warning::`/`::error::` workflow commands).

Owner: PowerShell CI policy — `.claude/rules/powershell/conventions.md`. Consumers derive on demand via the repo dep-graph edge scan (`tools/AGENTS.md` "Vertical slices").
