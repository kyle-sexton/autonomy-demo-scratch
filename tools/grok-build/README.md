# Grok Build tooling

Optional xAI Grok Build helpers — not required for repo bootstrap.

| Script | Purpose |
| ------ | ------- |
| [`check-availability.sh`](check-availability.sh) | JSON probe (`ready`, `reason`, install hint) |
| [`record-host-smoke.sh`](record-host-smoke.sh) | Optional host smoke + verify artifact (no Docker) |

SSOT: [`docs/grok-build/README.md`](../../docs/grok-build/README.md).
