---
type: ${TYPE}
date: ${DATE}
slug: ${SLUG}
topic: ${TOPIC}
session_id: ${SESSION_ID}
# <fill: handoff entries continuing a chain add `previous_handoff:` + `previous_session_id:` here; delete this line if first-in-slug or a non-handoff type>
---

# <fill: title — one line, e.g. "Handoff — <what shipped>">

<fill: body — fill the sections for this entry's `type` per the per-type content table in `work-artifact-reference.md` "Slice history directory format">
