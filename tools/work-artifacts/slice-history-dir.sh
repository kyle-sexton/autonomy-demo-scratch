# Slice history directory basename for .work/<slug>/journal/ entries.
# Value owner (format SSOT): docs/work-artifacts/work-artifact-reference.md
# "Slice history directory format". Sourced (not executed) by
# tools/work-artifacts/*.sh and skill scripts that resolve on-disk paths.

# shellcheck disable=SC2034  # consumed by sourcing scripts
SLICE_HISTORY_DIR_BASENAME="journal"
