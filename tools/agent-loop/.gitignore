node_modules/
build/

# Local toy workspace for `npm start` (bind-mount target when no CLI workspace path).
workspace/

# Per-run orchestrator logs — see logs/README.md
logs/

# Operator spend-safety attestation (never commit — see operator/README.md)
operator/spend-safety-attestation-cursor.json
operator/spend-safety-attestation-claude.json
operator/spend-safety-attestation-codex.json
operator/spend-safety-attestation-grok.json

# Machine-local secrets and run overrides (see .env.example, run.example.json).
.env
run.local.json
pools.local.jsonc

# Headless Cursor hooks overlay — generated per run from .claude/settings.json
runtime/
