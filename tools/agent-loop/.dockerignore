# Per-machine secrets and attestation (never in image context).
.env
operator/spend-safety-attestation-*.json
run.local.json

# Build / install / runtime output
node_modules
build
workspace
logs

# Tests and git metadata
**/*.test.ts
.git
