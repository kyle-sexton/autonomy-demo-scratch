You are ONE iteration of an autonomous implement loop. Work ONLY inside the current directory.

GOAL: create `out/regression-ok.txt` containing exactly the two characters `ok` (no newline required).

YOUR JOB THIS ITERATION:

1. If `out/regression-ok.txt` already exists and contains `ok`: create/modify/delete NOTHING and print exactly: <promise>NO_MORE_TASKS</promise>
2. Otherwise: create `out/regression-ok.txt` with content `ok`, then print exactly: <promise>NO_MORE_TASKS</promise>

Do not create, modify, or delete any other file. Print the promise token as the final line of your response.
